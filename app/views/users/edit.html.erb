<%= form_with(model: @user, url: user_path(params[:id]), local: true) do |f| %>
  <%= image_tag @user.image.url %>
  <p class="chose_image">
  <%= f.file_field :image %>
  </p>
  <p>
  <%= f.label :name %>
  <%= f.text_field :name %>
  </p>
  <%= f.label :email %>
  <%= f.email_field :email %>
  <%= f.submit "編集する" %>
<% end %>